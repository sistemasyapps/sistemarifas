ejecutar php artisan cache:forget home_static_data para limpiar cualquier valor legado antes de recibir tráfico real.
npm run build

1. Ejecutar php artisan migrate para aplicar las nuevas columnas de preórdenes.
  2. Configurar cron (* * * * * php /ruta/al/proyecto/artisan schedule:run) y mantener php artisan queue:work o un equivalente en producción
     para que ApproveOrderJob siga enviando correos y WhatsApp.
